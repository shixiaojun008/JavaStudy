package Interface;

public interface ICalculate extends IPrint{

    String add(int numA, int numB);

    String sub(int numA, int numB);

    String mul(int numA, int numB);

    String div(int numA, int numB);
}
